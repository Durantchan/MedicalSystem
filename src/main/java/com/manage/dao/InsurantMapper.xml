<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manage.dao.InsurantDao">
    <select id="findInsurantByIdcard" resultType="Map" parameterType="String">
        select * from insurance_user where idcard = #{idcard}
    </select>
</mapper>